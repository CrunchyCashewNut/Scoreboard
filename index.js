function homeAddOne() {
    sum = document.getElementById("display_home");
    sum.innerText = Number(sum.innerText) + 1;
}

function homeAddTwo() {
  sum = document.getElementById("display_home");
  sum.innerText = Number(sum.innerText) + 2;
}

function homeAddThree() {
  sum = document.getElementById("display_home");
  sum.innerText = Number(sum.innerText) + 3;
}

function guestAddOne() {
  sum = document.getElementById("display_guest");
  sum.innerText = Number(sum.innerText) + 1;
}

function guestAddTwo() {
  sum = document.getElementById("display_guest");
  sum.innerText = Number(sum.innerText) + 2;
}

function guestAddThree() {
  sum = document.getElementById("display_guest");
  sum.innerText = Number(sum.innerText) + 3;
}